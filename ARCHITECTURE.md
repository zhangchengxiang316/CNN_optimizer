# CNN 硬件加速器——各层原理详解

> 面向初学者的生动图解，帮助你彻底读懂项目里的每一个 `.v` 文件。

---

## 目录

1. [整体架构一览](#整体架构一览)
2. [pe.v — 处理单元（心脏）](#pev--处理单元心脏)
3. [line_buffer.v — 行缓存（传送带）](#line_bufferv--行缓存传送带)
4. [requantize.v — 重量化（缩放仪）](#requantizev--重量化缩放仪)
5. [relu.v — ReLU 激活（过滤器）](#reluv--relu-激活过滤器)
6. [input_data.v / weights_bias_*.v — 数据 ROM（仓库）](#input_datav--weights_bias_v--数据-rom仓库)
7. [conv1.v — 第一层普通卷积](#conv1v--第一层普通卷积)
8. [conv2_dw.v — 深度可分离卷积（独立刷碗工）](#conv2_dwv--深度可分离卷积独立刷碗工)
9. [conv3_pw.v — 逐点卷积（混调器）](#conv3_pwv--逐点卷积混调器)
10. [maxpool.v — 最大池化（淘汰赛）](#maxpoolv--最大池化淘汰赛)
11. [fc_layer.v — 全连接层（投票员）](#fc_layerv--全连接层投票员)
12. [sigmoid_lut.v — Sigmoid 激活（概率翻译官）](#sigmoid_lutv--sigmoid-激活概率翻译官)
13. [cnn_top.v — 顶层流水线（总调度员）](#cnn_topv--顶层流水线总调度员)
14. [tb_cnn_top.v — 仿真测试台](#tb_cnn_topv--仿真测试台)
15. [数据流全貌](#数据流全貌)
16. [如何替换为真实权重](#如何替换为真实权重)

---

## 整体架构一览

本项目实现的是一个运行在**芯片（FPGA/ASIC）**上的轻量级语音分类神经网络。
输入是一张 **MFCC 特征图**（可以理解为把一段音频"拍成"一张 30×10 的灰度照片），
输出是两个概率值（0 类 / 1 类）。

```
输入 MFCC
(1, 30, 10)
     │
     ▼
┌────────────┐
│   Conv1    │  11×7 普通卷积  →  提取局部语音纹理
│  + Act1    │  ReLU 激活
└────────────┘
(32, 20, 4)
     │
     ▼
┌────────────┐
│  Conv2 DW  │  3×3 深度可分离卷积  →  在每条通道上单独提特征
│  + Act2    │  ReLU 激活
└────────────┘
(32, 18, 2)
     │
     ▼
┌────────────┐
│  Conv3 PW  │  1×1 逐点卷积  →  32 通道之间混合信息
└────────────┘
(32, 18, 2)
     │
     ▼
┌────────────┐
│  MaxPool   │  2×2 最大池化  →  空间压缩，保留最突出的特征
└────────────┘
(32, 9, 1)
     │  flatten  →  288 个数字
     ▼
┌────────────┐
│  FC Layer  │  全连接层  →  综合所有特征，给出两个打分
└────────────┘
(2)
     │
     ▼
┌────────────┐
│  Sigmoid   │  输出概率
└────────────┘
result0, result1  (FP32)
```

**时序关系**（所有层顺序执行，上一层 `done` 触发下一层 `start`）：

```
clk ─────────────────────────────────────────────────────►
start ─┐
       └── Conv1 ──done──► Conv2 ──done──► Conv3 ──done──►
          MaxPool ──done──► FC ──done──► [捕获 Sigmoid 输出] ──► done
```

---

## pe.v — 处理单元（心脏）

**文件**：`pe.v`

### 📌 它是什么？

PE（Processing Element，处理单元）是整个加速器最小、最核心的运算单元，
只做一件事：**乘法 + 累加（MAC，Multiply-Accumulate）**。

### 🍕 生动类比

想象你在计算"一张披萨的总价格"：

```
总价 = 玛格丽特×2份 + 夏威夷×1份 + 海鲜×3份 + ...
```

每加一种披萨就做一次"乘（份数）+ 累加（到总价）"。
PE 就是这个"加法的手"——每个时钟周期做一次：

```
acc = acc + input_pixel × weight
```

### 🔬 代码解读

```verilog
always @(posedge clk or negedge rst_n) begin
    if (!rst_n)          acc_out <= 0;    // 复位：清零
    else if (clear)      acc_out <= 0;    // 同步清零（开始新窗口前）
    else if (en)
        acc_out <= acc_out + (data_in * weight_in);  // MAC！
end
```

| 信号 | 作用 |
|------|------|
| `data_in [7:0]` | INT8 输入像素（有符号，范围 -128 ~ 127） |
| `weight_in [7:0]` | INT8 权重（有符号，范围 -128 ~ 127） |
| `acc_out [31:0]` | INT32 累加器（32 位防止溢出） |
| `clear` | 开始计算新的输出点前清零 |
| `en` | 使能本拍运算 |

> **为什么用 INT32 存中间结果？**
> INT8 × INT8 最大结果是 127×127 ≈ 16000，但要累加 77 次（Conv1 的核大小），
> 最大可达 16000 × 77 ≈ 1,230,000，超过了 INT16 的范围（32767），
> 必须用 INT32（最大 21 亿）来安全存储。

---

## line_buffer.v — 行缓存（传送带）

**文件**：`line_buffer.v`

### 📌 它是什么？

卷积要在特征图上**滑动一个窗口**。比如 Conv1 用 11×7 的核，
每次需要同时看**连续 11 行**的数据。
行缓存的作用是把最新的 11 行数据"装在手边"，供卷积核随时取用，
避免反复去 SRAM 中翻找历史数据。

### 🎞️ 生动类比

想象一条**11 格的传送带**：

```
最新行  → [行0]
           [行1]
           [行2]
            ...
最旧行  → [行10]
```

每进来一行新数据：
- 传送带**向下移一格**（行1→行2，行2→行3 … 行10 直接丢掉）
- 新数据放在**最顶端行0**
- 任何时刻，传送带上都保存着**最新的连续 11 行**，
  卷积核可以立刻从任意位置取数据。

### 🔬 核心代码

```verilog
// 每收到完整一行（10 个像素）后执行"下移"：
for (i = KH-1; i > 0; i = i - 1)
    for (j = 0; j < W_IN; j = j + 1)
        row_mem[i][j] <= row_mem[i-1][j];   // 旧行向下移
// 新行放在顶部
for (j = 0; j < W_IN-1; j = j + 1)
    row_mem[0][j] <= cur_row[j];
row_mem[0][W_IN-1] <= in_data;              // 最后一个像素
```

### 内存布局

```
row_mem[0][0..9]   ← 最新行（刚进来的一行）
row_mem[1][0..9]   ← 次新行
row_mem[2][0..9]
...
row_mem[10][0..9]  ← 最旧行（即将被顶出去的行）
```

当 11 行全部就绪后，`rows_cnt >= KH`，行缓存宣布"准备好了"，
Conv1 的状态机就可以开始 MAC 计算。

---

## requantize.v — 重量化（缩放仪）

**文件**：`requantize.v`

### 📌 它是什么？

经过 MAC 累加后，中间结果是 INT32（范围数亿）。
但神经网络的下一层需要的是 INT8（-128 ~ 127）。
重量化模块负责把 INT32 **"压缩"回** INT8。

### 📏 生动类比

就像一把能量仪表：原来指针可能偏到 ±1,000,000，
但显示屏只有 256 个格。重量化做的是：

```
1. 加上偏置（bias）做细调
2. 整体除以一个比例因子（2^SHIFT = 256），相当于"缩小尺度"
3. 截断到 [-128, 127]（防止超量程）
```

### 🔬 代码解读

```verilog
wire signed [31:0] biased  = acc_in + bias_in_32bit;  // ① 加偏置
wire signed [31:0] shifted = biased >>> SHIFT;         // ② 算术右移（÷256）

always @(*) begin
    if      (shifted >  127)  data_out =  127;   // ③ 截断上限
    else if (shifted < -128)  data_out = -128;   // ③ 截断下限
    else                      data_out = shifted[7:0]; // 正常范围
end
```

> **算术右移 vs 逻辑右移**：
> `>>>` 是算术右移，负数最高位补 1（保持符号）；
> `>>` 是逻辑右移，永远补 0（负数会变成大正数，结果错误）。
> 量化时一定要用 `>>>`。

| 参数 | 含义 |
|------|------|
| `SHIFT = 8` | 等效比例 scale = 1/256 ≈ 0.0039 |
| 可修改为 `SHIFT = 7` | scale = 1/128，激活范围更大 |

---

## relu.v — ReLU 激活（过滤器）

**文件**：`relu.v`

### 📌 它是什么？

ReLU（Rectified Linear Unit，线性整流单元）是神经网络中最常用的激活函数：

```
output = max(0, input)
```

负数全变成 0，正数原样输出。

### 🚿 生动类比

就像一个**止回阀**：水（信号）只能正向流，反向（负数）直接关掉。
这个"过滤"操作让神经网络能表达非线性关系，
否则所有层叠加在一起等价于一个线性变换，无法学习复杂特征。

### 🔬 代码解读（极简！）

```verilog
assign data_out = data_in[7] ? 8'sd0 : data_in;
//                     ↑
//               符号位 = 1 说明是负数，输出 0；否则原样输出
```

INT8 有符号数：最高位（bit7）= 1 表示负数，= 0 表示非负数。
这一行代码用**硬件中最廉价的多路选择器（MUX）**实现了 ReLU，几乎零面积。

---

## input_data.v / weights_bias_*.v — 数据 ROM（仓库）

**文件**：`input_data.v`，`weights_bias_conv1.v`，`weights_bias_conv2.v`，`weights_bias_conv3.v`，`weights_bias_fc.v`

### 📌 它们是什么？

这五个文件是**只读存储器（ROM）**，分别存放：

| 文件 | 存储内容 | 大小 |
|------|----------|------|
| `input_data.v` | 一帧 MFCC 输入（1×30×10 = 300 个 INT8） | 300 B |
| `weights_bias_conv1.v` | Conv1 权重 + 偏置（2464 个 INT8 + 32 个 INT16） | 2.5 KB |
| `weights_bias_conv2.v` | Conv2 DW 权重 + 偏置（288 INT8 + 32 INT16） | 352 B |
| `weights_bias_conv3.v` | Conv3 PW 权重 + 偏置（1024 INT8 + 32 INT16） | 1 KB |
| `weights_bias_fc.v` | FC 权重 + 偏置（576 INT8 + 2 INT16） | 580 B |

### 📦 生动类比

把每个文件想象成一个**配方卡片盒**：

- `input_data.v` 是当前要处理的"食材"（输入特征图）
- `weights_bias_*.v` 是每道工序的"配方"（网络参数）

芯片在推理时只需**读取**这些 ROM，从不写入。

### 🔬 代码解读

```verilog
// 用伪随机数生成占位权重（实际部署时替换为训练好的值）
initial begin : init_weights
    integer i; integer x;
    x = 32'h00000001;               // 种子
    for (i = 0; i < 2464; i = i + 1) begin
        x = (x * 1664525 + 1013904223) & 32'hFFFFFFFF; // 线性同余生成器
        weights[i] = $signed(((x >> 16) % 9) - 4);     // 映射到 -4..4
    end
end
assign w_data = weights[w_addr];   // 组合逻辑读取（零延迟）
```

> 📝 **替换方式**：训练完神经网络后，用量化工具（如 PyTorch 的 QAT 流程）
> 导出 INT8 权重，然后把 `initial begin` 块里的值替换掉即可。
> 详见文末 [如何替换为真实权重](#如何替换为真实权重)。

---

## conv1.v — 第一层普通卷积

**文件**：`conv1.v`

### 📌 它做什么？

对输入特征图（1通道，30×10）用 **32 个不同的 11×7 滤波器**同时扫描，
输出 32 个通道、每个 20×4 的特征图。每个滤波器学习一种不同的语音纹理。

### 🎨 生动类比

想象你拿着 **32 种不同纹理的印章**，在一张 30×10 的图上逐格盖印。
每枚印章的格子数是 11×7 = 77 格，盖一次就要对着 77 个像素点各做一次乘法再加总。
每枚印章盖完后产生一张 20×4 的"印迹图"，32 枚印章产生 32 张印迹图。

### 🏗️ 硬件架构（最关键的设计！）

```
                   ┌─────────────────────────────────────────┐
输入像素流 ──────►  │  行缓存 (row_mem)                        │
(每拍 1 像素)       │  [行0]  [行1] ... [行10]  (11行×10列)    │
                   └──────────────────┬──────────────────────┘
                                      │ 广播同一个像素
               ┌──────────────────────▼──────────────────────┐
               │           脉动阵列（32 个 PE 并行）            │
               │  PE[0]:  acc[0] += pixel × w[0][k]           │
               │  PE[1]:  acc[1] += pixel × w[1][k]           │
               │  ...                                          │
               │  PE[31]: acc[31] += pixel × w[31][k]         │
               └──────────────────────┬──────────────────────┘
                                      │ (77 拍后)
               ┌──────────────────────▼──────────────────────┐
               │    重量化 + ReLU（32 拍，每拍处理一个通道）    │
               └──────────────────────┬──────────────────────┘
                                      │
                               output_buf[2560]
```

**关键优化**：
- 同一个输入像素**广播**给全部 32 个 PE → 32 路并行，一次 MAC 完成 32 个通道
- 行缓存消除重复 SRAM 访问 → 减少带宽

### 📅 状态机时序

```
IDLE ──start──► FEED（加载 11 行，110 拍）
                  │
                  ▼
              CLEAR（1 拍：清零 32 个累加器）
                  │
                  ▼
              MAC（77 拍：对 77 个核元素做并行 MAC）
                  │
                  ▼
              STORE（32 拍：重量化 + ReLU，写入输出缓冲）
                  │
                  ├── ow < 3 ──► CLEAR（滑动窗口向右一格）
                  │
                  └── ow == 3 ──► FEED（加载下一行）──► ...
                                    │
                                    └── oh == 19 ──► DONE
```

### 🔬 核心代码（MAC 阶段）

```verilog
// k_cnt = 0..76，对应核的第 kh 行 kw 列
wire [3:0] kh_cur = k_cnt / KW;              // 0..10
wire [2:0] kw_cur = k_cnt % KW;              // 0..6
wire signed [7:0] mac_pixel = row_mem[KH-1-kh_cur][ow + kw_cur];
                              // ↑ 从行缓存里取对应位置的像素

// 32 个 PE 同时运算（一行代码驱动 32 个乘加器！）
for (i = 0; i < C_OUT; i = i + 1)
    pe_acc[i] <= pe_acc[i] + mac_pixel * local_w[i][k_cnt];
```

> **为什么用 `KH-1-kh_cur` 而不是 `kh_cur`？**
> 行缓存里 `row_mem[0]` 是最新进来的行（对应窗口最底行），
> `row_mem[KH-1]` 是最旧的行（对应窗口最顶行）。
> 需要反向映射：核第 0 行（顶） → `row_mem[KH-1]`，
> 核第 10 行（底）→ `row_mem[0]`。

---

## conv2_dw.v — 深度可分离卷积（独立刷碗工）

**文件**：`conv2_dw.v`

### 📌 它做什么？

对 Conv1 输出的 **32 个通道**，每个通道**独立地**用自己的 3×3 滤波器扫描，
通道之间**不互相混合**。这叫做"深度卷积（Depthwise Convolution）"。

### 🍽️ 生动类比

饭店里 32 个刷碗工，每人只洗自己那一摞碗：

```
刷碗工0  只处理通道0，用自己的3×3刷子
刷碗工1  只处理通道1，用自己的3×3刷子
...
刷碗工31 只处理通道31，用自己的3×3刷子
```

好处：参数量极少（32×3×3 = 288 个权重，而普通卷积要 32×32×3×3 = 9216 个）。

### 📐 数学公式

对通道 `ch`，输出位置 `(oh, ow)`：

```
output[ch][oh][ow] = ReLU(
    Σ_{kh=0}^{2} Σ_{kw=0}^{2}
        input[ch][oh+kh][ow+kw] × weight[ch][kh×3+kw]
    + bias[ch]
)
```

### 🔬 核心代码

```verilog
// 每拍：对当前通道的当前核位置做一次 MAC
acc <= acc + mac_pixel * local_w[ch][k_cnt];

// 9 个核元素（k_cnt = 0..8）全部累加完后进入 STORE
if (k_cnt == K_SIZE - 1) state <= ST_STORE;
```

### 📊 对比普通卷积

| 对比项 | 普通 Conv1（11×7 核）| 深度卷积 Conv2（3×3 核）|
|--------|---------------------|------------------------|
| 计算量 | 32×1×11×7 = 2464 MAC/位置 | 32×1×3×3 = 288 MAC/位置 |
| 参数量 | 2464 个权重 | 288 个权重 |
| 跨通道 | ✅ 有 | ❌ 无 |

---

## conv3_pw.v — 逐点卷积（混调器）

**文件**：`conv3_pw.v`

### 📌 它做什么？

在每一个**空间位置**（18×2 = 36 个位置）上，
把来自 32 个输入通道的值**线性组合**成 32 个新值。
核大小是 1×1，所以只做通道混合，不做空间滑动。

### 🎚️ 生动类比

调音台有 32 个输入音轨，混音师调节每个旋钮的比例，
产生 32 路新的输出音轨。每个位置的"混音配方"（权重矩阵）完全一样。

```
输入通道 [ch0, ch1, ..., ch31]   (在同一个像素点)
             ↓ × 权重矩阵 32×32
输出通道 [ch0', ch1', ..., ch31'] (同一个像素点，通道已混合)
```

### 🏗️ 32-PE 并行设计

```
                       input[ic][oh][ow]  (ic = 0..31, 共32拍)
                              │ 广播
             ┌────────────────▼─────────────────────┐
             │  PE[0]:  acc[0] += pixel × w[0][ic]  │
             │  PE[1]:  acc[1] += pixel × w[1][ic]  │
             │  ...                                  │
             │  PE[31]: acc[31]+= pixel × w[31][ic] │
             └──────────────────────────────────────┘
                32 拍后所有 OC 的累加器同时完成
```

### 🔬 核心代码

```verilog
// 广播当前输入通道 ic 的像素给所有 32 个 PE
wire signed [7:0] mac_pixel = input_buf[ic * (H * W) + oh * W + ow];

// 32 个 PE 并行累加
for (i = 0; i < C_OUT; i = i + 1)
    pe_acc[i] <= pe_acc[i] + mac_pixel * local_w[i][ic];
```

---

## maxpool.v — 最大池化（淘汰赛）

**文件**：`maxpool.v`

### 📌 它做什么？

在每个通道上，用 **2×2 的窗口**每次跳 2 步（stride=2），
从 4 个像素中**只保留最大的那个**，把特征图从 18×2 压缩成 9×1。

### 🏆 生动类比

32 个赛区（通道）同时举办淘汰赛：
每 2×2 格的 4 名运动员（像素值）比赛，
最强的那一个晋级（进入输出），其余淘汰。

```
输入 (18行 × 2列)     输出 (9行 × 1列)
┌──┬──┐              ┌──────┐
│5 │3 │  → max(5,3,  │  7   │
│7 │2 │     7,2) =7  └──────┘
├──┼──┤
│1 │4 │  → max(1,4,  ┌──────┐
│6 │0 │     6,0) =6  │  6   │
└──┴──┘              └──────┘
  ...                   ...
```

### 🔬 代码解读

```verilog
reg signed [7:0] cur_max;                        // 当前 2×2 窗口内最大值

// ki 从 0 到 3，依次比较 4 个像素
if ($signed(pool_pixel) > $signed(cur_max))
    cur_max <= pool_pixel;                        // 更新最大值

if (ki == 3) begin
    output_buf[...] <= ($signed(pool_pixel) > $signed(cur_max))
                       ? pool_pixel : cur_max;   // 保存最终结果
    cur_max <= 8'sh80;                            // 重置为 -128（INT8 最小值）
end
```

> **为什么初始化 `cur_max = -128`？**
> 因为要找最大值，初始值必须小于所有可能的像素值，
> -128 是 INT8 的下界，保证第一个像素比较一定会更新 `cur_max`。

---

## fc_layer.v — 全连接层（投票员）

**文件**：`fc_layer.v`

### 📌 它做什么？

MaxPool 输出了 32 通道 × 9行 × 1列 = **288 个数**。
全连接层把这 288 个数通过加权求和，计算出**2 个分数**：
一个代表"这段语音属于类别0的程度"，另一个代表"属于类别1的程度"。

### 🗳️ 生动类比

288 位评委（输入特征），每位评委对两位候选人（类别0和类别1）各投一票（权重）。
把所有票加起来，票多的那位候选人获胜。

```
得分0 = w[0][0]×x[0] + w[0][1]×x[1] + ... + w[0][287]×x[287] + bias[0]
得分1 = w[1][0]×x[0] + w[1][1]×x[1] + ... + w[1][287]×x[287] + bias[1]
```

### 🏗️ 2-PE 并行设计

```
输入 x[ic]（ic = 0..287，共 288 拍）
       │ 广播
   ┌───▼───┐         ┌───────┐
   │ PE[0] │ 累加得分0 │ acc[0]│
   └───────┘         └───────┘
   ┌───▼───┐         ┌───────┐
   │ PE[1] │ 累加得分1 │ acc[1]│
   └───────┘         └───────┘
```

### 🔬 核心代码

```verilog
// 每拍：2 个 PE 同时对第 ic 个输入做 MAC
for (i = 0; i < C_OUT; i = i + 1)
    pe_acc[i] <= pe_acc[i] + input_buf[ic] * local_w[i][ic];

// 288 拍后进入 STORE：重量化，输出 2 个 INT8 分数
```

---

## sigmoid_lut.v — Sigmoid 激活（概率翻译官）

**文件**：`sigmoid_lut.v`

### 📌 它做什么？

把 FC 输出的两个 INT8 分数，通过 Sigmoid 函数变成 **0~1 之间的概率**：

```
sigmoid(x) = 1 / (1 + e^(-x))
```

输出越接近 1，越确信这个类别；越接近 0.5，越不确定。

### 📖 生动类比

Sigmoid 就像一个**自动翻译官**：
把任意大小的原始打分（比如 -100 到 +100）翻译成 0% ~ 100% 的置信度。

```
打分 -∞  →  概率 ≈ 0%   (完全不是这类)
打分  0  →  概率 = 50%  (不确定)
打分 +∞  →  概率 ≈ 100% (完全是这类)
```

### ⚡ 硬件实现：查找表（LUT）

在芯片上计算 `e^(-x)` 非常耗面积和时间。
我们的做法是提前把所有 256 个可能的 INT8 输入值对应的 Sigmoid 结果**打表存储**：

```verilog
reg [31:0] lut [0:255];  // 256 个 IEEE-754 FP32 结果

initial begin
    lut[8'd0]   = 32'h3F000000; // sigmoid(0.000) = 0.5000  (FP32)
    lut[8'd1]   = 32'h3F01FFF5; // sigmoid(0.031) = 0.5078
    lut[8'd2]   = 32'h3F03FFAB; // sigmoid(0.062) = 0.5156
    // ... 共 256 条
end

assign data_out = lut[data_in]; // 组合逻辑，零延迟
```

查表就像**查字典**：输入 INT8 作为"页码"，直接翻到对应的 FP32 概率值，
不需要任何运算，一个时钟周期（甚至无需时钟）就能得到结果。

### 📐 地址映射

| 输入 `data_in`（无符号索引） | 有符号 INT8 解读 | 对应实数值 x（每格 = 1/32）| sigmoid(x) |
|:---:|:---:|:---:|:---:|
| `0` | 0 | 0.000 | 0.5000 |
| `1` | +1 | +0.031 | 0.5078 |
| `127` | +127 | +3.969 | ≈ 0.9815 |
| `128` | -128 | -4.000 | ≈ 0.0180 |
| `255` | -1 | -0.031 | ≈ 0.4922 |

---

## cnn_top.v — 顶层流水线（总调度员）

**文件**：`cnn_top.v`

### 📌 它做什么？

把所有层像**流水线工厂**一样串联起来：
上一层完工（`done`），立刻触发下一层开工（`start`）。

### 🏭 流水线示意图

```
     外部          Conv1      Conv2DW     Conv3PW    MaxPool      FC
      │
  start ──────►  开始
                   │
                  done ──────► start
                                 │
                                done ──────► start
                                               │
                                              done ──────► start
                                                             │
                                                            done ──────► start
                                                                           │
                                                                          done
                                                                           │
                                                              ┌────────────▼───────────┐
                                                              │ Sigmoid + 输出 result0/1│
                                                              └────────────────────────┘
                                                                           │
                                                                      done ►──► 外部
```

### 🔬 关键代码

```verilog
// 每个时钟沿检查上一层的 done，生成下一层的 start 脉冲
always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        conv2_start <= 0; conv3_start <= 0;
        pool_start <= 0;  fc_start    <= 0;
    end else begin
        conv2_start <= 1'b0;  // 默认不触发
        ...
        if (conv1_done) conv2_start <= 1'b1;  // Conv1 完成 → 启动 Conv2
        if (conv2_done) conv3_start <= 1'b1;  // Conv2 完成 → 启动 Conv3
        if (conv3_done) pool_start  <= 1'b1;
        if (pool_done)  fc_start    <= 1'b1;
        if (fc_done) begin
            result0 <= sig_out0;   // 读取 Sigmoid 输出
            result1 <= sig_out1;
            done    <= 1'b1;       // 通知外部"推理完成"
        end
    end
end
```

### 📦 层间缓冲（接线箱）

```verilog
// 层间数据通过 wire 数组直接相连，没有额外寄存器
wire signed [7:0] conv1_out [0:32*20*4-1];  // 2560 B
wire signed [7:0] conv2_out [0:32*18*2-1];  // 1152 B
wire signed [7:0] conv3_out [0:32*18*2-1];  // 1152 B
wire signed [7:0] pool_out  [0:32*9*1-1];   //  288 B
wire signed [7:0] fc_out    [0:1];           //    2 B
```

---

## tb_cnn_top.v — 仿真测试台

**文件**：`tb_cnn_top.v`

### 📌 它做什么？

测试台是一个"测试夹具"，不会被综合成真实电路，只用于仿真验证。
它自动完成：

1. 产生 100 MHz 时钟（10 ns 周期）
2. 复位 4 拍后释放 `rst_n`
3. 拉高 `start` 触发推理
4. 等待 `done`（有 200,000 拍超时保护）
5. 打印各层关键输出值

### 🔬 时钟生成

```verilog
initial clk = 1'b0;
always #5 clk = ~clk;  // 每 5 ns 翻转一次 → 10 ns 周期 → 100 MHz
```

### 🔧 如何在 ModelSim 中运行

```tcl
# 方法一：直接执行 do 文件
do run_sim.do

# 方法二：手动命令
vlib work
vlog -sv *.v
vsim -t 1ns work.tb_cnn_top
run 2ms
```

---

## 数据流全貌

下表展示整个推理过程中数据的尺寸变化：

```
层              操作              输入尺寸              输出尺寸        Verilog 文件
─────────────  ────────────────  ────────────────────  ──────────────  ──────────────
(ROM)          输入特征图        —                     (1, 30, 10)     input_data.v
Conv1 + Act1   11×7 卷积 + ReLU  (1, 30, 10) = 300     (32, 20, 4)    conv1.v
                                  INT8                   = 2560 INT8
Conv2 + Act2   3×3 深度卷积+ReLU (32, 20, 4) = 2560    (32, 18, 2)    conv2_dw.v
                                  INT8                   = 1152 INT8
Conv3          1×1 逐点卷积      (32, 18, 2) = 1152    (32, 18, 2)    conv3_pw.v
                                  INT8                   = 1152 INT8
MaxPool        2×2 最大池化      (32, 18, 2) = 1152    (32, 9, 1)     maxpool.v
                                  INT8                   = 288 INT8
FC             全连接            288 INT8              2 INT8          fc_layer.v
Sigmoid        查表              2 INT8                2 FP32          sigmoid_lut.v
```

**总计算量估算**（MAC 次数）：

```
Conv1 :  32 OC × 1 IC × 11×7 核 × 20×4 位置  ≈ 194,480 MAC
Conv2 :  32 ch × 3×3 核 × 18×2 位置          ≈  10,368 MAC
Conv3 :  32 OC × 32 IC × 1×1 核 × 18×2 位置  ≈  36,864 MAC
FC    :  2 OC × 288 输入                      ≈     576 MAC
───────────────────────────────────────────────────────────
合计                                          ≈ 242,288 MAC
```

---

## 如何替换为真实权重

当前所有权重文件用伪随机数填充（只用于仿真验证流程的正确性）。
实际部署时，按以下步骤替换：

### 1. 训练并量化模型（Python 端）

```python
# 伪代码：PyTorch QAT 流程
import torch
model = MyCNNModel()
# ... 训练 ...
# 量化感知训练后导出 INT8 权重
quantized_model = torch.quantization.quantize_dynamic(model, dtype=torch.qint8)
weights_conv1 = quantized_model.conv1.weight().int_repr()  # Tensor of int8
```

### 2. 导出为 Verilog initial 块

```python
# 把 numpy 数组转成 Verilog 代码
def export_weights_verilog(arr, name="weights"):
    lines = [f"initial begin : init_{name}"]
    for i, v in enumerate(arr.flatten()):
        lines.append(f"    {name}[{i}] = 8'sd{int(v)};")
    lines.append("end")
    return "\n".join(lines)
```

### 3. 修改对应的 `.v` 文件

打开 `weights_bias_conv1.v`，把 `initial begin : init_weights` 块中的
LCG 循环替换成上面生成的 Verilog 代码。

### 4. 重新仿真验证

```tcl
# ModelSim
do run_sim.do
# 观察 result0/result1 的值，对照 Python 推理结果
```

---

> **总结**：整个项目用大约 800 行 Verilog 实现了一个完整的 INT8 量化 CNN 推理加速器。
> 核心设计思路是"**脉动阵列**"（32 个 PE 并行 MAC）+ "**行缓存**"（减少内存带宽），
> 每次推理约需 24,000 个时钟周期。替换为训练好的权重后，
> 即可在 FPGA 上实现语音命令识别。

---

*文档由 GitHub Copilot 自动生成，如有错误欢迎在 Issues 中反馈。*
